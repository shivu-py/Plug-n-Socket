# We will use this readme to communicate here about the ideas/plans, and changes we make

## ToDo:
- Need to add the refresh token in case the JWT expires

## Crosschecks & Testing:
- write JWT logic (under observation)
- design and build Register/Login API's (under observation)
  
## Done:
- Added the JWT Logic with access_token and built the register and login api.
- I have removed the "Thought" model as we discussed yesterday, we will first do the "Talks" part then we will see what else we can add

